%ul.nav
  - if signed_in?
    %li.support
      %a{ href: '#', id: 'Intercom' }
        = t('.support_link_html')
    - if current_user.has_remaining_invites?
      %li= link_to 'Invite a friend', new_invite_path
    %li= link_to 'Profile', current_user
    %li= link_to 'Sign out', sign_out_path, method: :delete
  - else
    %li= link_to 'Sign in', sign_in_path
