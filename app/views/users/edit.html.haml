- content_for :title, 'Edit Profile'

.profile
  = semantic_form_for current_user do |f|
    .avatar
      = image_tag current_user.avatar.url(:medium)
      = f.file_field :avatar

    = f.input :name
    = f.input :email
    = f.input :username
    = f.action :submit, label: 'Save', class: 'btn'
